# Railway Configuration — ARQ Worker (pas de health check HTTP)
# Le worker ARQ ne sert pas de requêtes HTTP, pas besoin de healthcheck

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
