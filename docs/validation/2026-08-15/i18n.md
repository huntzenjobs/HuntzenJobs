# Validation i18n — 12 août 2026

## Périmètre validé localement

- Un nouveau visiteur géolocalisé aux États-Unis reçoit `en`.
- Sans géolocalisation, `Accept-Language` sélectionne `fr`, `en`, `es` ou `pt` ; le repli neutre est `en`.
- Le pays prévaut sur le navigateur tant qu'aucune langue n'a été choisie manuellement.
- La locale calculée par `next-intl` initialise aussi le contexte client au premier rendu : aucun flash initial forcé en français.
- Le backend accepte `fr`, `en`, `es`, `pt` pour sa langue par défaut et rejette l'ancien code `de`.
- Les catalogues `fr`, `en`, `es` et `pt` contiennent exactement les mêmes 2 199 clés, sans clé supplémentaire.

## Preuves automatisées

- `npm run test:run -- --run __tests__/unit/i18n` : 8 tests réussis.
- `npm run sync-translations` : 2 199 clés présentes dans chacun des trois catalogues cibles, aucune traduction ajoutée.
- `npx tsc --noEmit` : réussi.
- `../.venv/bin/python -m pytest tests/unit/test_llm_model_configuration.py -q --tb=short` : 3 tests réussis.
- Ruff ciblé sur la configuration et son test : réussi.
- `git diff --check` : réussi.

## Restant avant validation staging

- Synchroniser automatiquement `profiles.preferred_language` vers le cookie lors d'une reconnexion afin d'établir la priorité complète « compte > choix explicite > pays > navigateur > anglais ».
- Tester le changement manuel et sa persistance sur connexion, déconnexion et multi-onglets.
- Exécuter les captures Playwright géolocalisées sur la version candidate déployée pour les États-Unis, la France, l'Espagne et le Portugal.
- Vérifier les formats dates, montants, fuseaux, pluriels et les chaînes codées en dur sur les parcours critiques.
- La configuration ESLint structurellement incompatible a été retirée. `npx eslint . --quiet` passe ; le nettoyage des 102 avertissements historiques non bloquants reste suivi dans la Task 12.
