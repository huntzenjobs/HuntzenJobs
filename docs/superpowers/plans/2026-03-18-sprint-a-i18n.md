# Sprint A — i18n HuntZen 28→100 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Atteindre 100/100 sur la dimension i18n — zéro string hardcodé, traductions complètes FR/EN/ES/PT, emails multilingues, registre de tutoiement unifié.

**Architecture:** Chaque tâche correspond à un subagent spécialisé. La dépendance critique est A1 (scanner) → A2+A3 (JSON fixes) → A4-A8 parallèles + A9 indépendant → A10 sync finale → A-REVIEW. Chaque sous-tâche suit le cycle READ → MODIFY → VALIDATE → COMMIT.

**Tech Stack:** next-intl (`useTranslations`), `messages/fr.json` (source de vérité), `messages/en.json`, `messages/es.json`, `messages/pt.json`, ruff (backend), `npx tsc --noEmit` (frontend), sonner (toasts)

---

## Carte des fichiers

### Créés
- `docs/audit/i18n-todo.md` — inventaire exhaustif des strings hardcodés (Tâche 1)

### Modifiés — JSON traductions
- `frontend-next/messages/fr.json` — toutes les nouvelles clés
- `frontend-next/messages/en.json` — 44 clés manquantes + corrections (Download, salons, etc.)
- `frontend-next/messages/es.json` — corrections balises `<g>` + sync sections manquantes
- `frontend-next/messages/pt.json` — correction "Sênior", `&#x0D;`, sync sections manquantes

### Modifiés — Pages dashboard (Tâche 3)
- `frontend-next/src/app/(dashboard)/candidatures/page.tsx`
- `frontend-next/src/app/(dashboard)/referral/page.tsx`
- `frontend-next/src/app/(dashboard)/expat/page.tsx`
- `frontend-next/src/app/(dashboard)/documents/page.tsx`
- `frontend-next/src/app/pricing/page.tsx`

### Modifiés — Pages publiques (Tâche 4)
- `frontend-next/src/app/about/page.tsx`
- `frontend-next/src/app/faq/faq-data.ts` (ou équivalent)
- `frontend-next/src/app/blog/page.tsx`
- `frontend-next/src/app/temoignages/testimonials-client.tsx`

### Modifiés — Toasts (Tâche 5)
- `frontend-next/src/contexts/subscription-context.tsx`
- `frontend-next/src/app/contact/page.tsx`
- `frontend-next/src/app/(dashboard)/profile/profile-client.tsx`
- `frontend-next/src/app/(dashboard)/documents/page.tsx`
- `frontend-next/src/app/pricing/page.tsx`
- `frontend-next/src/components/profile/subscription-card.tsx`

### Modifiés — CV Builder (Tâche 6)
- `frontend-next/src/components/cv/wizard/step1-upload.tsx`
- `frontend-next/src/components/cv/wizard/step2-analysis-type.tsx`
- `frontend-next/src/components/cv/wizard/step3-results.tsx`

### Modifiés — Backend (Tâche 8)
- `backend/src/services/email.py`

---

## Tâche 1 — A1 : Scanner i18n exhaustif (lecture seule)

**Objectif :** Produire la liste définitive de TOUS les strings hardcodés. Rien n'est modifié.

**Fichiers :**
- Créer : `docs/audit/i18n-todo.md`
- Lire : TOUS les fichiers `src/**/*.tsx`, `src/**/*.ts` du frontend

- [ ] **Étape 1 : Scanner les pages dashboard**

```bash
grep -rn --include="*.tsx" --include="*.ts" \
  -E '>[A-ZÀ-ÿ][a-zA-ZÀ-ÿ ]+<|placeholder="[^"]+"|aria-label="[^"]+"|title="[^a-z{"]' \
  frontend-next/src/app/\(dashboard\)/ 2>&1 | grep -v "t(" | grep -v "//\|*\|import\|className"
```

- [ ] **Étape 2 : Scanner les pages publiques**

```bash
grep -rn --include="*.tsx" --include="*.ts" \
  -E '>[A-ZÀ-ÿ][a-zA-ZÀ-ÿ ]+<|placeholder="[^"]+"|toast\.' \
  frontend-next/src/app/about/ frontend-next/src/app/faq/ \
  frontend-next/src/app/blog/ frontend-next/src/app/temoignages/ \
  frontend-next/src/app/pricing/ 2>&1 | grep -v "t(" | grep -v "//\|*\|import"
```

- [ ] **Étape 3 : Scanner les toasts hardcodés**

```bash
grep -rn --include="*.tsx" \
  -E 'toast\.(success|error|info|loading|warning)\("[^"]*[A-Za-zÀ-ÿ]' \
  frontend-next/src/ 2>&1 | grep -v "t("
```

- [ ] **Étape 4 : Scanner les placeholders CV builder**

```bash
grep -rn --include="*.tsx" \
  -E 'placeholder="[^"{]+[A-Za-zÀ-ÿ][^"]*"' \
  frontend-next/src/components/cv/wizard/ 2>&1
```

- [ ] **Étape 5 : Produire docs/audit/i18n-todo.md**

Format de chaque entrée :
```
## fichier:ligne
- Texte : "texte exact hardcodé"
- Clé proposée : namespace.sousNamespace.cle
- Type : toast | placeholder | text | aria-label | title
```

- [ ] **Étape 6 : Vérifier que le fichier est complet**

Compter les entrées et s'assurer qu'aucune des pages connues n'est manquante :
`candidatures/page.tsx`, `referral/page.tsx`, `expat/page.tsx`, `documents/page.tsx`, `pricing/page.tsx`, `about/page.tsx`, `subscription-context.tsx`

**Pas de commit — lecture seule.**

---

## Tâche 2 — A2+A3 : Fix traductions JSON (une seule passe)

**Objectif :** Corriger TOUS les problèmes dans les 4 fichiers JSON en une seule passe atomique.

**Fichiers :**
- Modifier : `frontend-next/messages/en.json`
- Modifier : `frontend-next/messages/pt.json`
- Modifier : `frontend-next/messages/es.json`
- Lire : `frontend-next/messages/fr.json` (source de vérité)

**Prérequis :** Tâche 1 terminée (i18n-todo.md disponible).

- [ ] **Étape 1 : Lire les fichiers JSON actuels**

```bash
cat frontend-next/messages/en.json | python3 -m json.tool > /dev/null && echo "EN OK"
cat frontend-next/messages/pt.json | python3 -m json.tool > /dev/null && echo "PT OK"
cat frontend-next/messages/es.json | python3 -m json.tool > /dev/null && echo "ES OK"
```

Résultat attendu : 3 lignes "OK" — si erreur JSON → corriger avant de continuer.

- [ ] **Étape 2 : Ajouter les 44 clés manquantes FR→EN dans en.json**

Sections à compléter dans `en.json` (comparer avec `fr.json`) :
- `hero.*` — tag, h1, h2, subtitle, ctaSearch, ctaDiscover, socialProof
- `trustBar.*`
- `howItWorks.*`
- `features.*`
- `footer.*`
- `pricing.taglines.*` (descriptions des plans)
- `dashboard.welcome.*`
- `dashboard.quickAction.*`
- `referral.*` (namespace complet)

Pour chaque clé manquante, ajouter la traduction EN naturelle et correcte (pas de traduction automatique non relue).

- [ ] **Étape 3 : Corriger les traductions erronées en.json**

```json
// Corrections obligatoires en.json :
"dashboard": {
  "documents": {
    "downloadButton": "Download"  // était "Upload" — erreur critique
  }
},
"salons": {
  "status": {
    "online": "Online"            // était "Inline"
  },
  "type": {
    "event": "Event"              // était "Property"
  },
  "search": "Search Events",     // était "Search Property"
  "noResults": "No events found", // était "No Property found"
  "details": "View Details",     // était "View Detail Logs"
  "news": "News"                  // était "News Search"
}
```

- [ ] **Étape 4 : Corriger pt.json**

```json
// Corrections obligatoires pt.json :
"filters": {
  "experienceLevels": {
    "senior": "Sênior"  // était "Idoso" — offensant
  }
},
"payment": {
  "cancel": {
    "tipLabel": "Você manterá o acesso até o fim do período"  // supprimer &#x0D;
  }
}
```

- [ ] **Étape 5 : Corriger es.json**

Supprimer toutes les balises `<g>` parasites dans `savedJobs.*` :
```bash
grep -n "<g>" frontend-next/messages/es.json
```
Remplacer les valeurs incriminées par le texte propre sans balises XML.

- [ ] **Étape 6 : Valider que les JSON sont valides**

```bash
cd frontend-next
node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8')); console.log('EN valid')"
node -e "JSON.parse(require('fs').readFileSync('messages/pt.json','utf8')); console.log('PT valid')"
node -e "JSON.parse(require('fs').readFileSync('messages/es.json','utf8')); console.log('ES valid')"
```

Résultat attendu : 3 lignes "valid".

- [ ] **Étape 7 : Diff pour vérification**

```bash
git diff --stat frontend-next/messages/
```

Vérifier que seuls les fichiers JSON attendus ont changé, pas fr.json.

- [ ] **Étape 8 : Commit**

```bash
git add frontend-next/messages/en.json frontend-next/messages/pt.json frontend-next/messages/es.json
git commit -m "fix(i18n): add 44 missing FR→EN keys + correct erroneous EN/PT/ES translations"
```

---

## Tâche 3 — A4 : Pages dashboard hardcodées

**Objectif :** Externaliser tous les strings des 5 pages dashboard sans useTranslations.

**Ordre de priorité :** pricing → candidatures → referral → expat → documents

**Pattern à appliquer sur chaque page :**
```tsx
// 1. Ajouter l'import en haut du fichier
import { useTranslations } from "next-intl"

// 2. Dans le composant
const t = useTranslations("dashboard.nomDeLaPage")

// 3. Remplacer chaque string
<h1>Mes candidatures</h1>  →  <h1>{t("title")}</h1>
placeholder="Rechercher..."  →  placeholder={t("search.placeholder")}
```

### Sous-tâche A4a — pricing/page.tsx

**Fichiers :**
- Modifier : `frontend-next/src/app/pricing/page.tsx`
- Modifier : `frontend-next/messages/fr.json` → namespace `pricing.*`
- Modifier : `frontend-next/messages/en.json` → namespace `pricing.*`

- [ ] **Étape 1 : Lire pricing/page.tsx**

```bash
grep -n '"[A-ZÀ-ÿ]' frontend-next/src/app/pricing/page.tsx | grep -v "//\|import\|className\|key=\|value="
grep -n 'toast\.' frontend-next/src/app/pricing/page.tsx
```

- [ ] **Étape 2 : Créer namespace pricing.* dans fr.json et en.json**

Ajouter sous `"pricing"` (qui existe déjà, compléter) :
```json
{
  "pricing": {
    "toasts": {
      "alreadyOnPlan": "Vous utilisez déjà ce plan",
      "mustBeLoggedIn": "Vous devez être connecté pour souscrire",
      "redirecting": "Redirection vers le paiement...",
      "sessionExpired": "Session expirée, veuillez vous reconnecter",
      "error": "Une erreur est survenue lors du paiement"
    }
  }
}
```

- [ ] **Étape 3 : Remplacer dans pricing/page.tsx**

Ajouter `useTranslations("pricing")` et remplacer chaque toast et text hardcodé.

- [ ] **Étape 4 : Valider**

```bash
cd frontend-next && npx tsc --noEmit 2>&1 | head -20
```

Résultat attendu : 0 erreur.

- [ ] **Étape 5 : Commit**

```bash
git add frontend-next/src/app/pricing/page.tsx frontend-next/messages/fr.json frontend-next/messages/en.json
git commit -m "fix(i18n): externalize pricing page strings"
```

### Sous-tâche A4b — candidatures/page.tsx

**Fichiers :**
- Modifier : `frontend-next/src/app/(dashboard)/candidatures/page.tsx`
- Modifier : `frontend-next/messages/fr.json` → namespace `dashboard.candidatures`
- Modifier : `frontend-next/messages/en.json` → namespace `dashboard.candidatures`

- [ ] **Étape 1 : Scanner les strings**

```bash
grep -n '"[A-ZÀ-ÿ]\|placeholder="\|toast\.' frontend-next/src/app/\(dashboard\)/candidatures/page.tsx | grep -v "//\|import"
```

- [ ] **Étape 2 : Créer namespace dans fr.json + en.json**

Structure minimale attendue :
```json
{
  "dashboard": {
    "candidatures": {
      "title": "Mes candidatures",
      "empty": "Aucune candidature pour le moment",
      "status": { "applied": "Postulé", "interview": "Entretien", "rejected": "Refusé" },
      "deleteConfirm": "Supprimer cette candidature ?",
      "errors": { "load": "Erreur lors du chargement", "delete": "Erreur lors de la suppression" }
    }
  }
}
```

- [ ] **Étape 3 : Ajouter useTranslations et remplacer les strings**

- [ ] **Étape 4 : Valider et commit**

```bash
cd frontend-next && npx tsc --noEmit 2>&1 | head -20
git add frontend-next/src/app/\(dashboard\)/candidatures/page.tsx frontend-next/messages/fr.json frontend-next/messages/en.json
git commit -m "fix(i18n): externalize candidatures page strings"
```

### Sous-tâches A4c (referral), A4d (expat), A4e (documents)

Répéter le même process (scan → créer clés → remplacer → validate → commit) pour :

**A4c — referral/page.tsx** → namespace `dashboard.referral`
**A4d — expat/page.tsx** → namespace `dashboard.expat`
**A4e — documents/page.tsx** → namespace `dashboard.documents` (compléter l'existant)

Pour chaque page :
- [ ] Scanner les strings hardcodés
- [ ] Créer les clés dans fr.json ET en.json simultanément
- [ ] Remplacer dans le composant avec `useTranslations`
- [ ] `npx tsc --noEmit` — 0 erreur
- [ ] Commit `fix(i18n): externalize [page] strings`

---

## Tâche 4 — A5 : Pages publiques hardcodées

**Objectif :** Externaliser les pages about, faq, blog, temoignages. Créer fichiers MDX pour privacy/terms.

**Fichiers :**
- Modifier : `frontend-next/src/app/about/page.tsx`
- Modifier : `frontend-next/src/app/faq/faq-data.ts` (ou `faq-client.tsx`)
- Modifier : `frontend-next/src/app/blog/page.tsx`
- Modifier : `frontend-next/src/app/temoignages/testimonials-client.tsx`

### Sous-tâche A5a — about/page.tsx (~100+ strings)

- [ ] **Étape 1 : Identifier le volume exact**

```bash
wc -l frontend-next/src/app/about/page.tsx
grep -c '"[A-ZÀ-ÿ]' frontend-next/src/app/about/page.tsx
```

- [ ] **Étape 2 : Créer namespace about.* dans fr.json et en.json**

Structure (adapter au contenu réel) :
```json
{
  "about": {
    "hero": { "title": "...", "subtitle": "..." },
    "mission": { "title": "...", "description": "..." },
    "team": { "title": "...", "subtitle": "..." },
    "values": { "title": "...", "items": { ... } },
    "stats": { "users": "...", "satisfaction": "..." },
    "cta": { "title": "...", "button": "..." }
  }
}
```

- [ ] **Étape 3 : Remplacer dans about/page.tsx**

```tsx
import { useTranslations } from "next-intl"
const t = useTranslations("about")
// <h1>Notre mission</h1>  →  <h1>{t("mission.title")}</h1>
```

- [ ] **Étape 4 : Valider et commit**

```bash
cd frontend-next && npx tsc --noEmit 2>&1 | head -20
grep -n '"[A-ZÀ-ÿ]' src/app/about/page.tsx | grep -v "t(\|//\|import" # doit être vide
git add src/app/about/page.tsx messages/fr.json messages/en.json
git commit -m "fix(i18n): externalize about page strings (~100 keys)"
```

### Sous-tâches A5b (faq), A5c (blog), A5d (temoignages)

Même process pour chaque page. Namespaces : `faq.*`, `blog.*`, `testimonials.*`

Pour la FAQ, si les données sont dans un fichier `.ts` séparé (ex: `faq-data.ts`), créer un hook qui retourne les données via `useTranslations` plutôt qu'un tableau statique.

- [ ] Scanner strings de chaque page
- [ ] Créer namespace dans fr.json + en.json
- [ ] Remplacer avec useTranslations
- [ ] `npx tsc --noEmit` — 0 erreur
- [ ] Commit par page

### Sous-tâche A5e — Privacy/Terms → MDX

**Raison :** Textes légaux de 2000-3000 mots — impossible à gérer en clés i18n. MDX par langue est la bonne approche.

- [ ] **Étape 1 : Vérifier l'état actuel**

```bash
ls frontend-next/src/app/privacy/ frontend-next/src/app/terms/
head -20 frontend-next/src/app/privacy/page.tsx
```

- [ ] **Étape 2 : Créer les fichiers MDX localisés**

```
frontend-next/content/legal/privacy.fr.mdx  → copie du contenu FR actuel
frontend-next/content/legal/privacy.en.mdx  → version EN (traduire le texte légal)
frontend-next/content/legal/terms.fr.mdx
frontend-next/content/legal/terms.en.mdx
```

- [ ] **Étape 3 : Adapter les pages pour charger le MDX selon la locale**

```tsx
// privacy/page.tsx
import { getLocale } from "next-intl/server"
// Charger le fichier MDX correspondant à la locale
```

- [ ] **Étape 4 : Valider et commit**

```bash
cd frontend-next && npx tsc --noEmit 2>&1 | head -20
git add src/app/privacy/ src/app/terms/ content/legal/
git commit -m "fix(i18n): convert privacy and terms pages to localized MDX"
```

---

## Tâche 5 — A6 : Toasts hardcodés (hors admin)

**Objectif :** Remplacer tous les `toast.xxx("texte FR")` hors admin par `toast.xxx(t("clé"))`.

**Note :** Les toasts admin (admin/coupons, admin/logs, admin/dashboard) sont exclus — l'admin est FR uniquement.

**Fichiers prioritaires (hors admin) :**
- Modifier : `frontend-next/src/contexts/subscription-context.tsx`
- Modifier : `frontend-next/src/app/contact/page.tsx`
- Modifier : `frontend-next/src/app/(dashboard)/profile/profile-client.tsx`
- Modifier : `frontend-next/src/app/(dashboard)/documents/page.tsx`
- Modifier : `frontend-next/src/components/profile/subscription-card.tsx`

- [ ] **Étape 1 : Inventaire final des toasts hors admin**

```bash
grep -rn --include="*.tsx" \
  -E 'toast\.(success|error|info|loading)\("[^"]*[A-Za-zÀ-ÿ]' \
  frontend-next/src/ | grep -v "t(\|/admin/"
```

- [ ] **Étape 2 : Corriger subscription-context.tsx**

Ce fichier est un Context Client Component — `useTranslations` est utilisable.

```tsx
// Ajouter import
import { useTranslations } from "next-intl"

// Dans le composant SubscriptionProvider
const t = useTranslations("subscription")

// Remplacer :
toast.success("Abonnement synchronisé", {...})   →  toast.success(t("synced"), {...})
toast.error("Synchronisation impossible", {...})  →  toast.error(t("syncFailed"), {...})
toast.error("Session expirée", {...})             →  toast.error(t("sessionExpired"), {...})
```

Ajouter dans fr.json + en.json :
```json
{
  "subscription": {
    "synced": "Abonnement synchronisé",
    "syncFailed": "Synchronisation impossible",
    "sessionExpired": "Session expirée"
  }
}
```

- [ ] **Étape 3 : Corriger les autres fichiers**

Répéter pour chaque fichier de la liste : lire → identifier les toasts → créer les clés → remplacer.

- [ ] **Étape 4 : Vérification finale**

```bash
grep -rn --include="*.tsx" \
  -E 'toast\.(success|error|info|loading)\("[^"]*[A-Za-zÀ-ÿ]' \
  frontend-next/src/ | grep -v "t(\|/admin/"
```

Résultat attendu : **0 ligne** (toutes les occurrences sont remplacées).

- [ ] **Étape 5 : Type check**

```bash
cd frontend-next && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Étape 6 : Commit**

```bash
git add frontend-next/src/contexts/subscription-context.tsx \
  frontend-next/src/app/contact/page.tsx \
  frontend-next/src/app/\(dashboard\)/profile/profile-client.tsx \
  frontend-next/src/app/\(dashboard\)/documents/page.tsx \
  frontend-next/src/components/profile/subscription-card.tsx \
  frontend-next/messages/fr.json frontend-next/messages/en.json
git commit -m "fix(i18n): externalize 35 hardcoded toast messages (hors admin)"
```

---

## Tâche 6 — A7 : CV Builder placeholders

**Objectif :** Externaliser les ~20 placeholders hardcodés dans les 3 fichiers step-*.tsx.

**Fichiers :**
- Modifier : `frontend-next/src/components/cv/wizard/step1-upload.tsx`
- Modifier : `frontend-next/src/components/cv/wizard/step2-analysis-type.tsx`
- Modifier : `frontend-next/src/components/cv/wizard/step3-results.tsx`

- [ ] **Étape 1 : Scanner les placeholders**

```bash
grep -n 'placeholder="\|aria-label="\|title="' \
  frontend-next/src/components/cv/wizard/step*.tsx | grep -v "t("
```

- [ ] **Étape 2 : Créer namespace cvBuilder.* dans fr.json + en.json**

```json
{
  "cvBuilder": {
    "step1": {
      "dropzone": "Déposez votre CV ici ou cliquez pour parcourir",
      "formats": "PDF, DOC, DOCX — max 10 Mo",
      "uploading": "Chargement en cours..."
    },
    "step2": {
      "title": "Type d'analyse",
      "quick": "Analyse rapide",
      "deep": "Analyse approfondie"
    },
    "step3": {
      "score": "Score ATS",
      "download": "Télécharger le rapport"
    }
  }
}
```

- [ ] **Étape 3 : Remplacer avec useTranslations("cvBuilder")**

- [ ] **Étape 4 : Valider et commit**

```bash
cd frontend-next && npx tsc --noEmit 2>&1 | head -20
grep -n 'placeholder="[^{]' frontend-next/src/components/cv/wizard/step*.tsx | grep -v "t("
# Résultat attendu : 0 ligne
git add frontend-next/src/components/cv/wizard/ frontend-next/messages/fr.json frontend-next/messages/en.json
git commit -m "fix(i18n): externalize CV builder step placeholders"
```

---

## Tâche 7 — A8 : Unification tutoiement/vouvoiement

**Objectif :** Appliquer un registre unifié sur TOUT le dashboard.

- [ ] **Étape 1 : Identifier le registre de la landing page**

```bash
grep -o '"[^"]*tu\b[^"]*"\|"[^"]*vous\b[^"]*"' frontend-next/messages/fr.json | head -20
```

Décision : si la landing utilise le **tutoiement** ("ton CV", "tu mérites"), le dashboard doit aussi.

- [ ] **Étape 2 : Trouver toutes les incohérences**

```bash
# Chercher "vous" dans les clés fr.json (si tutoiement dominant)
grep -n '"vous\|Vous' frontend-next/messages/fr.json | head -30
# Ou chercher "tu\|ton\|ta\|te " si vouvoiement dominant
grep -n '"tu \|ton \|ta \|te "' frontend-next/messages/fr.json | head -30
```

- [ ] **Étape 3 : Corriger les valeurs dans fr.json**

Remplacer selon la décision de registre. Exemple (si tutoiement) :
```json
"Votre CV a été analysé"  →  "Ton CV a été analysé"
"Vous n'avez pas encore..."  →  "Tu n'as pas encore..."
```

- [ ] **Étape 4 : Vérification sur 10 pages représentatives**

Lire 10 pages du dashboard et confirmer la cohérence du registre sur les strings traduits.

- [ ] **Étape 5 : Valider et commit**

```bash
node -e "JSON.parse(require('fs').readFileSync('frontend-next/messages/fr.json','utf8')); console.log('JSON valid')"
git add frontend-next/messages/fr.json
git commit -m "fix(i18n): unify tutoiement/vouvoiement across fr.json"
```

---

## Tâche 8 — A9 : Emails multilingues (backend — indépendant)

**Objectif :** Ajouter paramètre `language` sur les 11 templates email backend.

**Fichiers :**
- Modifier : `backend/src/services/email.py`

- [ ] **Étape 1 : Lire la structure actuelle de email.py**

```bash
grep -n "def send_\|def create_\|def email_" backend/src/services/email.py
wc -l backend/src/services/email.py
```

- [ ] **Étape 2 : Identifier les 11 templates**

```bash
grep -n '"subject"\|"html"\|template' backend/src/services/email.py | head -30
```

- [ ] **Étape 3 : Ajouter le paramètre language**

Pattern à appliquer sur chaque fonction d'envoi d'email :

```python
# Avant
async def send_welcome_email(email: str, name: str) -> bool:
    subject = "Bienvenue sur HuntZen !"
    html = f"<p>Bonjour {name}...</p>"

# Après
TRANSLATIONS = {
    "welcome": {
        "fr": {
            "subject": "Bienvenue sur HuntZen !",
            "greeting": "Bonjour",
        },
        "en": {
            "subject": "Welcome to HuntZen!",
            "greeting": "Hello",
        },
    },
    # ... autres templates
}

async def send_welcome_email(email: str, name: str, language: str = "fr") -> bool:
    lang = language if language in ("fr", "en") else "fr"
    tr = TRANSLATIONS["welcome"][lang]
    subject = tr["subject"]
    html = f"<p>{tr['greeting']} {name}...</p>"
```

- [ ] **Étape 4 : Appliquer sur les 11 templates**

Un template à la fois, avec le même pattern.

- [ ] **Étape 5 : Valider**

```bash
cd backend && ruff check . 2>&1 | head -20
python -c "from src.services.email import send_welcome_email; print('Import OK')"
```

Résultat attendu : 0 erreur ruff, import OK.

- [ ] **Étape 6 : Commit**

```bash
git add backend/src/services/email.py
git commit -m "fix(i18n): add language parameter to 11 email templates (FR+EN)"
```

---

## Tâche 9 — A10 : Sync ES/PT (après Tâches 3-7)

**Objectif :** Synchroniser les sections manquantes en ES et PT avec le FR.

**Fichiers :**
- Modifier : `frontend-next/messages/es.json`
- Modifier : `frontend-next/messages/pt.json`

**Prérequis :** Tâches 3, 4, 5, 6, 7 terminées (nouveaux namespaces dans fr.json créés).

- [ ] **Étape 1 : Identifier les sections manquantes**

```bash
# Extraire toutes les clés de fr.json
node -e "
const fr = require('./frontend-next/messages/fr.json');
const es = require('./frontend-next/messages/es.json');
const keys = (obj, prefix='') => Object.keys(obj).flatMap(k =>
  typeof obj[k] === 'object' ? keys(obj[k], prefix+k+'.') : [prefix+k]
);
const missing = keys(fr).filter(k => !k.split('.').reduce((o,p)=>o?.[p], es));
console.log('ES missing:', missing.length, 'keys');
missing.slice(0,20).forEach(k => console.log(' -', k));
"
```

- [ ] **Étape 2 : Synchroniser les sections prioritaires en ES**

Sections obligatoires : `welcome.*`, `quickAction.*`, `dashboard.referral.*`, toutes les nouvelles sections créées en Tâches 3-7.

Ajouter les traductions ES correspondantes.

- [ ] **Étape 3 : Synchroniser PT**

Même process pour `messages/pt.json`.

- [ ] **Étape 4 : Vérifier la complétude**

```bash
node -e "
const fr = require('./frontend-next/messages/fr.json');
const pt = require('./frontend-next/messages/pt.json');
const keys = (obj, prefix='') => Object.keys(obj).flatMap(k =>
  typeof obj[k] === 'object' ? keys(obj[k], prefix+k+'.') : [prefix+k]
);
const missing = keys(fr).filter(k => !k.split('.').reduce((o,p)=>o?.[p], pt));
console.log('PT missing keys:', missing.length);
"
```

Résultat attendu : 0 clé manquante (ou un nombre documenté pour v2).

- [ ] **Étape 5 : Valider JSON et commit**

```bash
node -e "JSON.parse(require('fs').readFileSync('frontend-next/messages/es.json','utf8')); console.log('ES valid')"
node -e "JSON.parse(require('fs').readFileSync('frontend-next/messages/pt.json','utf8')); console.log('PT valid')"
git add frontend-next/messages/es.json frontend-next/messages/pt.json
git commit -m "fix(i18n): sync ES/PT with FR — add missing welcome, quickAction, referral sections"
```

---

## Tâche 10 — A-REVIEW : Audit final i18n

**Objectif :** Valider que le score i18n atteint 100/100 avant de passer au Sprint B.

- [ ] **Étape 1 : Scanner résiduel**

```bash
# Vérifier qu'il ne reste aucun toast hardcodé hors admin
grep -rn --include="*.tsx" \
  -E 'toast\.(success|error|info|loading)\("[^"]*[A-Za-zÀ-ÿ]' \
  frontend-next/src/ | grep -v "t(\|/admin/"

# Vérifier qu'il ne reste aucun string hardcodé visible dans les pages dashboard
grep -rn --include="*.tsx" \
  '>[A-ZÀ-ÿ][a-zA-ZÀ-ÿ ]' \
  frontend-next/src/app/\(dashboard\)/ | grep -v "t(\|//\|import\|className\|{t\|key="
```

Résultats attendus : 0 ligne pour chaque commande.

- [ ] **Étape 2 : Vérifier la complétude des JSON**

```bash
# fr.json → en.json : 0 clé manquante
node -e "
const fr = require('./frontend-next/messages/fr.json');
const en = require('./frontend-next/messages/en.json');
const keys = (obj, prefix='') => Object.keys(obj).flatMap(k =>
  typeof obj[k] === 'object' ? keys(obj[k], prefix+k+'.') : [prefix+k]
);
const missing = keys(fr).filter(k => !k.split('.').reduce((o,p)=>o?.[p], en));
if (missing.length) { console.log('FAIL — EN missing:', missing.length); missing.forEach(k=>console.log(' -',k)); }
else { console.log('✅ EN is complete'); }
"
```

- [ ] **Étape 3 : Type check complet**

```bash
cd frontend-next && npx tsc --noEmit 2>&1
```

Résultat attendu : 0 erreur.

- [ ] **Étape 4 : Vérifier les JSON backend**

```bash
cd backend && ruff check . 2>&1 | head -20
```

Résultat attendu : 0 erreur.

- [ ] **Étape 5 : Rapport de score**

Documenter le résultat dans `docs/audit/sprint-a-review.md` :
```markdown
# Sprint A i18n — Review Report
Date : 2026-03-18

## Résultats scan
- Toasts hardcodés hors admin : 0
- Strings hardcodés pages dashboard : 0
- Clés EN manquantes vs FR : 0
- Erreurs TypeScript : 0
- Erreurs ruff backend : 0

## Score estimé i18n : 100/100

## Actions restantes (v2 si applicable)
[liste des exceptions documentées]
```

- [ ] **Étape 6 : Commit final Sprint A**

```bash
git add docs/audit/sprint-a-review.md
git commit -m "docs(audit): Sprint A i18n complete — score 100/100"
```

---

## Ordre d'exécution final

```
Wave 1 (séquentiel obligatoire) :
  Tâche 1 (A1 — Scanner)
  ↓
  Tâche 2 (A2+A3 — JSON fixes)
  ↓
Wave 2 (parallélisable) :
  Tâche 3 (A4 — Dashboard pages)    ← peut tourner en parallèle avec Tâche 4, 5, 6, 7, 8
  Tâche 4 (A5 — Pages publiques)
  Tâche 5 (A6 — Toasts)
  Tâche 6 (A7 — CV Builder)
  Tâche 7 (A8 — Tutoiement)
  Tâche 8 (A9 — Emails backend)    ← indépendant, aucun conflit
  ↓ (toutes terminées)
Wave 3 :
  Tâche 9 (A10 — Sync ES/PT)
  ↓
Wave 4 :
  Tâche 10 (A-REVIEW — Audit final)
```

## Règles absolues d'exécution

1. **Jamais passer à la tâche suivante avec une erreur TypeScript ou ruff non résolue**
2. **Valider les JSON avec `node -e "JSON.parse(...)"` avant chaque commit**
3. **Ne jamais committer `docs/plans/`**
4. **Pas de `Co-Authored-By` dans les commits**
5. **Un seul fichier de composant modifié à la fois — valider avant de passer au suivant**
6. **Admin pages exclues du scope i18n** (internal tool, FR only acceptable)
